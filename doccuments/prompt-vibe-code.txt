Quản lý yêu cầu:
  + sắp xếp giảm dần theo thời gian gần nhất, hiển thị theo ngày hôm nay/ chọn 1 ngày trong tháng/ tuần này, tháng này: phân làm 4 nhóm mới nhận, đang xử lý, đã xử lý, sắp tới hạn;
  + xem được chi tiết yêu cầu: nội dung email, tệp đính kèm
  + 1 thanh ngang ở đầu email hiển thị gợi ý nhân đã được AI dự đoán (yêu cầu-mức độ ưu tiên), hiển thị đọc danh sách nhân viên phòng ban xử lý nhận đó (sắp xếp giảm dần task đang nhận/ chọn sẵn nhân viên ít task nhất) 2 nút dùng dự đoán-từ chối dự đoán;
  + Nút tùy chọn tự phân nhân: Hiển thị danh sách nhân yêu -> chọn nhân phù hợp, hiển thị danh sách nhân viên phòng ban -> chọn phòng ban;
  + Nút xác nhận phân công (gọi api phân công tự động or phân công thủ công)
  + Lọc yêu cầu theo task đang xử lý, đã xử lý, sắp đến hạn - thêm nút thông báo nhắc nhở (gửi thông báo đến email-thông báo trên hệ thống)
Giúp tôi triển khai các chức năng trên (triển khai cho manage-requests-page.jsx) 
- Nếu có dùng modal dialog thì dùng modal dialog quản lý bởi userModalDialogStore
- Sử dụng ui tiếng việt (tránh lỗi tiếng việt có dấu)
- Luôn dùng css style của bootstrap và sử dụng react bootstrap, chỉ viết thêm scss theo scss module. Nếu dùng icon thì ưu tiên dùng icon của react-icons nên dùng của bootstrap
- @tanstack/react-table dùng cho table
- nếu tách component thì lưu trong components/manage-requests
- Không sửa model trong backend, chỉ sửa khi cần thiết


Yêu cầu của Officer:
- Triển khai trang officer-requests.jsx
- sử dụng tiếng việt có dấu cho ui
- Dùng css style của bootstrap và các component của react-bootstrap là chính. Chỉ viết scss module khi cần thiết
- Trang tổng quan các yêu cầu được gán cho officer gồm thanh tìm kiếm, filter, table các request
- Table thì dùng useTable và có cột action gồm xem (chỉ xem thông tin tổng quát) và chi tiết (nút này sẽ tải chi tiết xem ở trang mới, chưa cần triển khai), có phân trang dùng PaginationControl của react bootstrap pagination control
- Xem tổng quan mở xem bằng Offcanvas của react-bootstrap
- Đừng sửa filter.jsx với, search-bar.jsx của tôi, khi cần thiết mới sửa.


Chi tiết yêu cầu - officer
- sử dụng tiếng việt có dấu cho ui
- Dùng css style của bootstrap và các component của react-bootstrap là chính. Chỉ viết scss module khi cần thiết
- Layout gồm 2 phần bên trái và bên phải. Bên trái to hơn, ùng nav tab gồm 2 phần chi tiết (Nội dung, tiêu đề, email sinh viên, nhãn. Và có khu hiển thị để hiển thị các tệp đính kèm) và trao đổi (là đoạn chát trao đổi giữa sinh viên với Officer, có phần cho nhập tin nhắn, có thể cho gửi kèm tệp đính kèm). Còn bên phải thì hiển thị trạng thái, độ ưu tiên, ghi chú nội bộ (trong database của tôi chưa có nên tạm thời có khung text ghi coming soon) các button có chức năng như: đánh dấu hoàn thành, trả lời tạm thời khi bấm vào thì toast comming soon.


Viết lại manage-request-page.jsx
- sử dụng tiếng việt có dấu cho ui
- Dùng css style của bootstrap và các component của react-bootstrap là chính. Chỉ viết scss module khi cần thiết
- Dùng filter-client.jsx của tôi: chỉ fetch khi timeRange param đổi giá trị, còn cái status và priority thì filter trên dữ liệu lấy về ở client thôi
- Có phân trang nhưng ở client, cho chọn số lượng, từ 20 trở lên.
- Thiết kế lại layout: có 3 tab (dùng nav tab) tương ứng với dữ liệu trả về từ getAllRequests
- Vẫn dùng @tanstack/react-table và userModalDialogStore


Triển khai tiếp notifications.jsx
- sử dụng tiếng việt có dấu cho ui
- Dùng css style của bootstrap và các component của react-bootstrap là chính. Chỉ viết scss module khi cần thiết
- Hiển thị tối đa 10 thông báo. Làm cho thông báo có type là REQUEST_ASSIGNED trước, các type khác triển khai sau. có icon, có thời gian, có nội dung
- Khi nhấn vào thông báo sẽ đi thì toast đã xem (tạm như thế), và mark là đã đọc, thêm chức năng đánh dấu tất cả đã đọc
- Chỉ thông báo khi nhấn vào chuông


Quản lý account
- sử dụng tiếng việt có dấu cho ui
- Dùng css style của bootstrap và các component của react-bootstrap là chính. Chỉ viết scss module khi cần thiết
- Table thì dùng useTable, có phân trang dùng PaginationControl của react bootstrap pagination control
- dùng filter.jsx. cho các trường có sẵn, còn lại để triển khai sau
- Tạo component lưu vào components/common truyền vào childrend và allow role để render ra childrend
- Luôn hiển thị cho chọn số trang từ 10, 20,... ,50
- Nếu có dùng modal dialog thì dùng modal dialog quản lý bởi userModalDialogStore (cho xem chi tiết và tạo mới)
- dùng useForm và tạo schemas bằng zod cho tạo account mới và chỉnh sửa account, chỉ cập nhập những trường nào thay đổi trong dùng (dirtyFields)
